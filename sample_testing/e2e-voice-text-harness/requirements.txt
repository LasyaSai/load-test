httpx>=0.27.0
pyyaml>=6.0.1
gtts>=2.5.1       # fixture generation (optional, falls back to macOS say)
websockets>=12.0  # WebSocket client for backend communication
pytest>=7.0.0     # Testing framework
pytest-asyncio>=0.20.0  # Async support for pytest
pytest-cov>=3.0.0  # Code coverage reporting
pytest-mock>=3.0.0  # Mocking support for pytest